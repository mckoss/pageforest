namespace.lookup('org.startpad.dom.test').defineOnce(function (ns) {
    var base = namespace.lookup('org.startpad.base');
    var dom = namespace.lookup('org.startpad.dom');

    ns.addTests = function (ts) {

        ts.addTest("na", function(ut) {
        });

    };
});
